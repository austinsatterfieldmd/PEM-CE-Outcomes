# CME Question Explorer Dashboard - Backend Dependencies

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Data validation
pydantic>=2.0.0

# Database
# SQLite is built into Python, no additional package needed

# CORS middleware included in fastapi

# NOTE: The following dependencies are disabled to reduce Vercel bundle size
# Uncomment when enabling Okta authentication:
# python-jose[cryptography]>=3.3.0

# NOTE: pandas/openpyxl were removed as they're not used in API routes
# If data processing is needed in serverless functions, consider lighter alternatives
